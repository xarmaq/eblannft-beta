{
  "version": "1.0.0",
  "notes": "Rollback to v1.0.0 monolithic build. The 1.5.5 wrapper-loader caused an immediate ClassCastException (bool→void) on plugin enable, so the GitHub channel is reverted to the last known-good single-file build until the wrapper bug is found and fixed.",
  "files": [
    "eblannft.plugin"
  ]
}
